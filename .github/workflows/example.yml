name: Example Workflow

on:
  workflow_dispatch

jobs:
  run_tests:
    runs-on: ubuntu-latest

    steps:

      - name: checkout files from repo
        uses: actions/checkout@v2

      - name: demo shell commands
        run: | 
          echo "Hello!"
          pwd
          ls -la

      - name: demo a second step
        run: 
          echo "Are you still here?"
          python --version
